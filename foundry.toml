[profile.default]
src = "."
test = "."
out = "out"
libs = ["lib"]
solc_version = "0.8.20"

# Governance often requires heavy computation; adjust optimizer if needed
optimizer = true
optimizer_runs = 200
